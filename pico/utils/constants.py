import os

CONFIG_DIR = os.path.join('/', 'etc', 'pico-wizard')
CONFIG_FILE = 'pico.conf'
CONFIG_FILE_PATH = os.path.join(CONFIG_DIR, CONFIG_FILE)

LOG_FILE_PATH = os.path.join('/', 'var', 'log', 'pico-wizard', 'pico.log')
